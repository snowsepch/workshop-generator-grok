# 小教室Grok版 - 除錯待辦紀錄

**最後更新**：2026 年（依本次對話）

## 已完成的重要修正（本次會話）
- 新增「一鍵清除」按鈕（主題庫標題區 + 清除列，含紅色提示與 disabled 狀態）。
- 多選年齡層邏輯修正：頁數固定為一份（不隨年齡層倍增），使用 `getEffectiveAge()` 智慧合併語氣與例子（agendaNote、handsNote、closeNote、hook、pitch 等）。
- 「用官網連結產生主題」功能移除，改為極簡「從官網快速新增」混合模式：
  - 貼 Apple 官網文字 → 解析 → 直接開「新增自訂主題」表單並預填（名稱、副標、功能、分類自動預選）。
  - 內建 `fixAppleCasing()` 強制正確產品大小寫（iPhone、iPad、MacBook、AirPods、AirTag、Apple Intelligence 等）。
  - 適合多人高量使用，操作最簡單。
- 分類系統優化：
  - 左側篩選從按鈕群改為下拉式（直覺、不混淆）。
  - renderTopics() 自動群組顯示 + 智慧推斷（未分類主題依名稱自動歸 iPhone/iPad/Mac...）。
  - 明確修正 AirTag 分類錯誤（原因 `n.includes('ai')` 誤判，現已加 `airtag` 優先 + 嚴謹 AI 檢查：需空格或完整詞如 'apple intelligence' / 'gemini'）。
  - 預填邏輯同步更新分類推斷。
- 產品大小寫規則全面強制（解析、預填、顯示端）。
- 備課講稿改為條列式 + 細項說明結構（時間分配、開場、各主題亮點、動手實作流程、多群體提醒、結尾話術），更容易閱讀。
- 更新 README.md 為正確版紀錄（v4 commit 範例 + 詳細其他實用性優化清單）。
- 移除舊複雜官網連結功能殘留程式碼與說明。

## 目前已知 / 待驗證項目（建議下次從這裡開始）
1. **分類推斷完整性**：目前依名稱關鍵字，建議未來為所有硬編碼主題明確加上 `category` 欄位，避免推斷出錯。檢查是否還有其他邊緣案例（如含 "ai" 但非 AI 的主題）。
2. **快速新增解析 robustness**：目前是簡單行分割 + 特徵提取，遇到複雜官網頁面可能抓不全。測試更多真實 Apple 官網文字（尤其是新產品），補強解析規則。
3. **多選年齡層內容品質**：智慧合併後的文字（尤其是 agendaNote / handsNote）在實際投影片上是否自然？是否需要更進階的模板或條件式組合？
4. **自訂主題持久化與分享**：localStorage 目前是單機，團隊多人使用時建議定期匯出 JSON 共用。是否需要內建「匯出全部自訂」一鍵功能或版本標記？
5. **UI 細節**：
   - 分類下拉 + 群組顯示在不同解析度/瀏覽器是否仍直覺？
   - 「一鍵清除」按鈕在有大量主題時的位置與視覺是否夠明顯？
   - 快速新增 modal 的提示文字是否足夠清楚？
6. **大小寫規則覆蓋**：確認所有輸出（海報、社群文案、備課講稿、PPTX）都走 fixAppleCasing，沒有遺漏。
7. **線上版同步**：每次本地修改後，記得覆蓋 repo 的 index.html 並 push，讓 https://snowsepch.github.io/workshop-generator-grok/ 保持最新。
8. **新功能建議 / 未來擴充**（非 bug）：
   - 支援更多自訂欄位（例如難度等級、建議時長）。
   - 批次從多個官網文字一次解析多個主題。
   - 在 summary 或生成按鈕旁顯示「目前使用有效年齡層：XXX」更清楚的提示。
   - 考慮把分類 + tags 也應用到硬編碼主題的篩選與推薦。

## 下次除錯提醒方式（強烈建議這樣做）
直接在對話開頭清楚說：
- 「繼續上次的除錯」或「基於 v4 狀態，現在有新問題：...」
- 附上：
  - 問題症狀 + 截圖（尤其是側邊欄、modal、生成結果）
  - 重現步驟（例如：選哪些主題 + 哪些年齡層 → 按什麼 → 看到什麼）
  - 預期 vs 實際行為
  - 如果是分類問題，註明目前篩選是哪個分類、哪個主題出錯
  - 本機檔案路徑（雖然我們知道是這個專案）

這樣我就能立刻回憶上下文、對應到具體程式碼位置，直接針對性除錯，不用重問細節。

如果有這個「除錯待辦.md」，可以直接說「看除錯待辦.md 的第 X 項繼續」。

---

**目前建議優先順序（如果要繼續）**：
1. 驗證 AirTag 在「AI」篩選時確實不會出現（已加明確 category + 嚴謹推斷）。
2. 測試「從官網快速新增」用真實 Apple 官網文字，確認預填 + 分類預選正確。
3. 檢查多選年齡層（例如家長族群 + 銀髮族）生成後的實際投影片內容是否自然。
4. 補強備課講稿裡的多群體提醒文字。

有任何新問題或想繼續哪一項，隨時開頭直接說「繼續上次的除錯」，我會立刻接手處理。這個專案持續性很高，這樣紀錄方式能讓除錯效率最高！